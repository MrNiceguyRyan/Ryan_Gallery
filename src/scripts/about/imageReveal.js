/* ============================================================
   Effect · Image clip-path reveal (动效 4)  — TODO (next step)
   Each [data-img-reveal] img opens from a centre slit (clip-path
   inset) while scaling down to 1, scrubbed across a pinned range
   for a breathing feel. Stub for now.
   ============================================================ */
export function initImageReveal(/* ctx */) {
  /* implemented in step 动效 4 */
}
