/* ============================================================
   Effect · Works slideshow (动效 6)  — TODO (next step)
   Pinned full-screen project stage; the side list drives the active
   project, swapping the background image and clip-revealing the
   title + index block. Stub for now.
   ============================================================ */
export function initWorksSlider(/* ctx */) {
  /* implemented in step 动效 6 */
}
