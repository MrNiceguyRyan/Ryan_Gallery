/* ============================================================
   Effect · Hero card expand (动效 1)  — TODO (next step)
   Pin the hero, scrub the central black card scaling up until it
   fills the viewport and swallows the portrait behind it.
   Stub for now so the entry wiring stays clean.
   ============================================================ */
export function initHeroExpand(/* ctx */) {
  /* implemented in step 动效 1 */
}
