/* ============================================================
   Effect · "Carve it / Into / Memory" converge (动效 5)  — TODO
   Flip.getState() on the spread layout, swap a layout class so the
   three words collapse to one line + the centre portrait scales to
   full-bleed, then Flip.from() bound to a pinned scrub. Theme flips
   white→black mid-way. The signature moment. Stub for now.
   ============================================================ */
export function initFlipConverge(/* ctx */) {
  /* implemented in step 动效 5 */
}
