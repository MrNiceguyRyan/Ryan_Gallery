/* ============================================================
   Effect · Word & character reveal (动效 2 + 3)  — TODO (next step)
   splitWordsReveal(el) / splitCharsReveal(el): wrap text into
   .word / .char spans (dim → full contrast) and scrub their
   colour with scroll, staggered. Stub for now.
   ============================================================ */
export function initTextReveal(/* ctx */) {
  /* implemented in steps 动效 2 & 3 */
}
